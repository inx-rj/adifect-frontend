import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#2472FC",
    },
    success: {
      main: "#20A144",
    },
    error: {
      main: "#FA2D20",
    },
    warning: {
      main: "#20A144",
    },
  },
});
